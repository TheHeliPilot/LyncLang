// Note: This features Phase 3 syntax which is currently in the roadmap
def main(): int {
    maybe_ptr: own? int = alloc 42;

    i: int = 5;

    if(some(maybe_ptr)){}

    match maybe_ptr {
        some(maybe_ptr): {}
        _: {}
    };

    free maybe_ptr;
    return 0;
}