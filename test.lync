// Test memory leak detection
def main(): int {
    ptr: own int = alloc 42;
    free ptr;
    return 0;
}

def foo(): int {
    x: own int = alloc 100;
    free x;  // Correct - freed before return
    return 0;
}
